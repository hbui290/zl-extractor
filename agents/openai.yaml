interface:
  display_name: "ZL Extractor"
  short_description: "Export Zalo chats and links safely"
  default_prompt: "Use $zl-extractor to export a named Zalo chat with organized messages, links, and pins; include media only when explicitly requested."
